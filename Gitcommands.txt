1. git init
2. git status
3. git add file name
4. git commit -m "message"
5. git add *.* or . >> will add everything



Git: Git is a common vs system where as GIT hub or bitbucket are remote repository what we use to store our code as 
we cant keep it our local also it will be only accessible for owner whereas if we keep it at remote repo then anyone
having access can use and update or add.

Login to Git Hub go to repository and copy repo path: https://github.com/asmramanpiet/GitJenkinsIntegration.git
 now run below command:
6. git  remote add origin https://github.com/asmramanpiet/GitJenkinsIntegration.git
7. git push -u origin master

8. git log : for details
9. git --help